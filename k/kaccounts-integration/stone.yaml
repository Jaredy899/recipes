# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : kaccounts-integration
version     : "26.04.1"
release     : 20
upstreams   :
    - https://download.kde.org/stable/release-service/26.04.1/src/kaccounts-integration-26.04.1.tar.xz : 014a079cead78292dc830fb85a4e6bf539dca4e5d7f67498d6ea2e0048c6c352
homepage    : https://kde.org/
license     : GPL-2.0-or-later
summary     : System to administer web accounts - shared library
description : |
    System to administer web accounts - shared library
builddeps   :
    - cmake(AccountsQt6)
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(KF6CoreAddons)
    - cmake(KF6DBusAddons)
    - cmake(KF6I18n)
    - cmake(KF6KCMUtils)
    - cmake(KF6KIO)
    - cmake(KF6Wallet)
    - cmake(QCoro6)
    - cmake(Qt6CorePrivate)
    - cmake(Qt6Qml)
    - cmake(SignOnQt6)
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install

    # # remove docs
    # rm -rf %(installroot)/%(datadir)/doc
packages    :
    - "%(name)-qt6":
        summary: System to administer web accounts - Qt6 bindings
        description: |
            System to administer web accounts - Qt6 bindings
        paths:
            - /usr/lib/qt6/plugins/
            - /usr/lib/qt6/qml/
            - /usr/share/applications/kcm_kaccounts.desktop
            - /usr/share/locale/
            - /usr/share/qlogging-categories6/
            - /usr/lib/libkaccounts6.so.*

    - "%(name)-qt6-devel":
        summary: System to administer web accounts - Qt6 bindings - devel package
        description: |
            System to administer web accounts - Qt6 bindings - devel package
        rundeps:
            - "%(name)-qt6"
            - binary(intltool-merge)
            - cmake(AccountsQt6)
            - cmake(SignOnQt6)
        paths:
            - /usr/include/KAccounts6/
            - /usr/lib/cmake/KAccounts6/
            - /usr/lib/libkaccounts6.so
