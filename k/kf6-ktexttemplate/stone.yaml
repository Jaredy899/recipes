# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : kf6-ktexttemplate
version     : "6.26.0"
release     : 12
upstreams   :
    - https://download.kde.org/stable/frameworks/6.26/ktexttemplate-6.26.0.tar.xz : 8b84643c32caf58812fec5a910a1fb98865bc7f91e778af860fa98b79d0ff038
homepage    : https://community.kde.org/Frameworks
license     :
    - LGPL-2.0-or-later
    - LGPL-2.1-or-later
summary     : Separates the structure of documents from their data
description : |
    The goal of KTextTemplate is to make it easier for application developers to separate the structure of documents from the data they contain, opening the door for theming and advanced generation of other text such as code.
builddeps   :
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(Qt6Qml)
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install
